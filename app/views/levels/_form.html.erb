<div class="container">
<%= form_with(model: level) do |form| %>
  <% if level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(level.errors.count, "error") %> prohibited this level from being saved:</h2>

      <ul>
        <% level.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field my-2">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>


  <div class="actions mt-2">
    <%= form.submit "Add Level", class:'bg-blue-400 p-2 border-2 border-rounded-md font-bold' %>
  </div>
<% end %>
</div>
