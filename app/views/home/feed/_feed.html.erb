<div class="md:mr-72">
    <% @courses.each do |course| %>
        <div class=" w-full  justify-center mx-auto bg-white border rounded-xl ">
            <div class="flex w-full font-semibold m-2 text-xl">
                <%= course.title %>
            </div>
            <div class="container text-md" id="courseId">
                <%= course.content %>
                <%= render "home/feed/shares_button" %>
            </div>
        </div>
        <div class="my-2"></div>
    <% end %>
</div>